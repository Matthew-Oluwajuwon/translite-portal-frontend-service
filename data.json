{
  "page_1": {
    "endpoint": "/api/v1/adminUser/add",
    "fields": {
      "firstName": "string",
      "lastName": "string",
      "email": "string",
      "password": "string",
      "adminRoleName": "string"
    }
  },
  "page_2": {
    "endpoint": "/api/v1/adminUser/all",
    "description": "This will be a table that will show the list of admin users created, also on this table there shud be an action where dey can enable and disable an admin user",
    "data": [
        {
          "id": 2,
          "email": "usertest@gmail.com",
          "firstName": "User",
          "lastName": "Test"
        },
        {
          "id": 1,
          "email": "superadmin@user.com",
          "firstName": "Super",
          "lastName": "Admin"
        }
      ]
  },
  "page_3": {
    "endpoint": "/api/v1/adminUser/resetAdminPassword/{username}",
    "description": "This might not be a page but we need where an admin can reset there password, maybe a button to click"
  },
  "page_4": {
    "endpoint": "/api/v1/adminUser/resetAdminPassword",
    "description": "This will have a single field of password where the admin can enter a new password, just one field, it must be inside the page because the admin has to be logged in before they can see this page",
    "field": {
        "password": "string"
    }
  }
}
